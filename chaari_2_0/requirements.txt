# CHAARI 2.0 – Dependencies

# Core
requests>=2.31.0

# Audio – Microphone
sounddevice>=0.4.6
numpy>=1.24.0

# Audio – Speech-to-Text (CPU, offline fallback)
faster-whisper>=1.0.0

# Audio – Speech-to-Text (lightweight online, Dell)
SpeechRecognition>=3.10.0

# Audio – STT: Chrome Web Speech (ASUS)
selenium>=4.15.0
webdriver-manager>=4.0.0
langdetect>=1.0.9
mtranslate>=1.8

# Audio – Text-to-Speech (Edge TTS, neural voices)
edge-tts>=6.1.0
pygame>=2.5.0

# Audio – Text-to-Speech (offline fallback)
pyttsx3>=2.90

# Audio – Wake Word
openwakeword>=0.6.0

# Audio – Keyboard Shortcut
keyboard>=0.13.5

# System monitoring (Layer 2 – Tool Truth)
psutil>=5.9.0

# Cryptography (Phase 3 – Crypto Layer)
cryptography>=41.0.0

# Cloud LLM (Groq API – fast inference, Ollama fallback when limit reached)
groq>=0.4.0

# Agentic RAG – RAPTOR (Hierarchical Tree Retrieval)
chromadb>=0.4.0
sentence-transformers>=2.2.0
scikit-learn>=1.3.0
pypdf>=3.0.0
tiktoken>=0.5.0
